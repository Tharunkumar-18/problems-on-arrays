#include<stdio.h>
int main(){
    int n,i,j,b,m=0,k;
    scanf("%d\n",&n);
    int a[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&b);
        a[i]=b;
    }
    for(i=0;i<n;i++){
        m=0;
        for(j=i+1;j<n;j++){
            if(a[i]==a[j]){
            m = m+1;
            }
        if(m>0)
        k++;
        }      
        
    }
    if(k>1){
        printf("contains duplicates");
    }
    else{
        printf("no duplicate values");
    }
    
}
